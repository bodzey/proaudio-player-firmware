[Journal]
# Runtime logs are diagnostic data, not appliance state. Keep them in RAM so
# routine logging cannot amplify SD-card writes or leave a persistent journal
# half-written after abrupt power loss.
Storage=volatile
RuntimeMaxUse=16M
RuntimeMaxFileSize=4M
