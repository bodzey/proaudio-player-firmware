[Manager]
# systemd writes the BCM2835 hardware watchdog at half this interval. If PID 1
# or the whole userspace stalls long enough to miss the deadline, the board
# resets without requiring a remote operator.
RuntimeWatchdogSec=10s
